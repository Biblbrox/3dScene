#version 330 core

// Texture coords
in vec2 TextureCoords;

// Texture number
uniform sampler2D m_textureNum;

out vec4 FragColor;

void main()
{
    FragColor = texture(m_textureNum, TextureCoords);
}